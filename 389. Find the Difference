class Solution {
    public char findTheDifference(String s, String t) {
        int a=0;
        int b=0;
        for(char i:s.toCharArray()) a+=(i-'a');
        for(char i:t.toCharArray()) b+=(i-'a');
        return (char)(Math.abs(a-b)+'a');
    }
}
