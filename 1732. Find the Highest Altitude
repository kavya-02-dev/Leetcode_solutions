class Solution {
    public int largestAltitude(int[] gain) {
        int max=0;
        int curr=0;
        for(int num:gain){
            
            
            if(curr>max) max=curr;
            curr=curr+num;
            
        }
        if(curr>max) max=curr;
        return max;
    }
}
