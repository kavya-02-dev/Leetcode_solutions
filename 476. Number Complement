class Solution {
    public int findComplement(int num) {
        int mask=0;
        int x=num;
        while(x>0){
            x=(x>>1);
            mask=(mask<<1) |1;
        }
        return (num^mask);
    }
}
