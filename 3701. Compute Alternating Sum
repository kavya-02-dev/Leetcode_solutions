class Solution {
    public int alternatingSum(int[] nums) {
        int even=0;
        int odd=0;
        for(int i=0;i<nums.length;i+=2){
            even+=nums[i];
            if(i+1<nums.length) odd+=nums[i+1];
        }
        return even-odd;
    }
}
