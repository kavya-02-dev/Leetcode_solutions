class Solution {
    public boolean hasTrailingZeros(int[] nums) {
        int c=0;
        for(int x:nums){
            if((x&1)==0){
                c++;
                if(c>=2) return true;
            }
        }
        return false;
    }
}
