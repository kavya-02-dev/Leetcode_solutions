class Solution {
    public String reversePrefix(String s, int k) {
        char x[]=s.toCharArray();
        for(int i=0;i<k/2;i++){
            char t=x[i];
            x[i]=x[k-i-1];
            x[k-1-i]=t;
        }
        return new String(x);
    }
}
