class Solution {
    public int duplicateNumbersXOR(int[] nums) {
        int xor=0;
       int freq[]=new int[51];
        for(int num:nums){
            if(freq[num]==1) xor=xor^num;
            freq[num]++;
        }
        return xor;
    }
}
