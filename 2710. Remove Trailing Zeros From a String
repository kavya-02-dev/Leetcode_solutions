class Solution {
    public String removeTrailingZeros(String num) {
        int n=0;
        for(int i=num.length()-1;i>=0;i--){
            if(((int)num.charAt(i)-'0')==0) n++;
            else break;
        }
        return num.substring(0,num.length()-n);
    }
}
