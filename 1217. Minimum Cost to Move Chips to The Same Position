class Solution {
    public int minCostToMoveChips(int[] position) {
        
        int odd=0;
        int even=0;
        for(int x: position ){
            if((x&1)==0) odd++;
            else even++;
        }
        return Math.min(odd,even);
    }
}
