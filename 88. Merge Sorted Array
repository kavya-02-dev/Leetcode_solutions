class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int a[]= new int[m+n];
        int i=m;
        
        for(int x: nums2) nums1[i++]=x;
        Arrays.sort(nums1);
        
    }
}
