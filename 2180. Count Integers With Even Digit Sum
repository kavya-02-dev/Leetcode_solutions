class Solution {
    public int countEven(int num) {
        int n=num;
        int c=0;
        while(n>0){
            c+=n%10;
            n/=10;
        }
        if(c%2==0) return (num)/2;
        return (num-1)/2;
    }
}
