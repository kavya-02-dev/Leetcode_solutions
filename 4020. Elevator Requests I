class Solution {
    public int elevatorRequests(int n, int[] requests) {
        int prev=0;
        int sum=0;
        for(int i: requests){
            sum+=Math.abs(prev-i);
            prev=i;
        }
        return sum;
    }
}
